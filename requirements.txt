numpy
opencv-python-headless
rasterio
pyyaml
fastapi
uvicorn
pydantic
scipy
# SPICE geometry: needed to place the LROC NAC image on the Moon, since its
# label carries no geographic metadata. Optional - the rest of the platform
# runs without it and reports the capability as unavailable.
spiceypy
